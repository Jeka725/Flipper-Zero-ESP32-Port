# AGENTS.md

## Что это за проект
Прошивка для [Порт Flipper-Zero-ESP32]. Собирается через читхаь актионс

## Как собирать
- Сборка: `pio run`
- Конкретное окружение: `pio run -e <env>`
- Тесты: `pio test`

## Правила
- Если сборка упала — исправь и перезапусти.
- Считай каждую неудачную попытку. Лимит: 3.
- Если после 3 попыток сборка не проходит — остановись, напиши отчёт.
- НЕ трогай файлы в папке `/legacy/` без спроса.

## Границы (что можно и нельзя)
✅ Всегда: запускать сборку перед коммитом
⚠️ Спросить: удаление файлов, изменение CI
🚫 Никогда: пуш в main напрямую, работа с секретами
